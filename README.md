# ライフマネープラン

家計簿ではなく「人生設計・人生シミュレーター」。
お金を管理するのではなく、人生で何がいつ起きて、そのときお金がどうなるかを一枚で見るためのツール。

## コンセプト

- お金は「点」ではなく「流れ」として見る
- 過去・現在・未来を同じ画面で見る
- 未来予測は「当てる」のではなく「比較する」(年利・退職年齢などのシナリオを変えて比較する)
- 節約ではなく「人生の選択」を支援する
- 最終的に「あといくら使っていいか」が分かることを目指す

## 構成(ページ)

| ファイル | 内容 | 状態 |
|---|---|---|
| `index.html` | 投資シミュレーション。現在の資産・月々のキャッシュフロー(ボーナス含む)・想定年利から将来の資産推移をグラフ化 | 完成 |
| `timeline.html` | 人生キャッシュフロー。年×収入・支出・資産の横スクロールグリッド。家族の生年から年齢を自動計算し、収入・支出はフォワードフィル(入力した年以降ずっとその額。途中の年を変えればそこから先が切り替わる)、NISA・個別株などの投資資産を年率%で複利計算して総資産に統合。年次↔月次プランナーの相互反映にも対応 | Phase 1-3 完成 |
| `kakeibo.html` | 家計簿。日々の支出記録+固定費+月合計を月単位で記録。分類(カテゴリ)は人生キャッシュフローと共通で、大項目ごとに整理・追加・削除できる | 完成 |

## 今後の予定(フェーズ)

1. [x] 投資シミュレーション(複利計算+グラフ)
2. [x] 人生キャッシュフロー Phase 1(年齢・収入・支出・資産の基本グリッド)
3. [x] ライフイベント行の自動反映(「ライフイベントの設定」で名称・年・金額を登録すると、その年のイベント費→支出合計→資産に自動加算され、タイムラインのイベント行に名称表示)
4. [x] 学年自動表示・年金自動切替(子の年齢から学年(小1〜大4)を自動表示。「年金の設定」でパパ・ママの受給開始年齢+年額を設定すると、その年齢になった年から収入に自動加算)
5. [x] NISA・投資信託・定期などの資産別テーブル(行ごとに年率%を設定して複利計算、総資産に統合)
6. [x] 保険テーブル(「保険の設定」で各社を登録し、保険料は毎年の支出に統合、受取年・受取額を指定すると学資保険の満期受取などをその年の収入に加算)
7. [x] 家計簿タブ(月次の実績記録、分類のカスタマイズ対応)
8. [x] 家計簿の実績を人生キャッシュフローの年間支出に反映(分類を「大項目+分類」の共通ストアにまとめ、家計簿⇔タイムラインで相互反映。家計簿に「①今月の金額のみフローに反映」「②今月以降すべてに反映」ボタンを用意)
9. [x] 収入・支出のフォワードフィル(入力した年以降ずっとその額。途中の年を変えればそこから先が切り替わる。定年は0を入力)
10. [x] 月次プランナー(年次タイムラインと月別入力の相互反映)

## ローカルで使う(ネット不要)

- `ローカルで使う.md` を参照。フォルダ一式(HTML 3つ + `chart.umd.min.js`)を端末にコピーし、ブラウザで開くだけ。
- 入力データはその端末のブラウザ内(localStorage)のみに保存され、外部送信されない。

## 未決定・検討中

- アプリとしてのリリース(スマホアプリ化・配布)。今はWeb(ローカル/GitHub Pages)。

## UIメモ

- サービス名表示は「デザイン４ライフ」。共通の左サイドバーで 家計簿 / 月次キャッシュフロー / 年次タイムライン / 投資シミュレーション を切替。
- 月次と年次は別表示(サイドバーで切替)。グリッドは表内スクロールで見出し・項目名・年齢/イベント行を固定。
- 入力欄は円のカンマ区切り表示。月をいじると年次も12ヶ月合計に自動更新。
- 設定の最下部に「データのバックアップ(書き出し/読み込み)」。3アプリ分を1ファイルで保存・復元。

## 技術メモ

- 単一HTMLファイル(HTML+CSS+JS)、ビルド不要
- グラフ表示: Chart.js(`chart.umd.min.js` を同梱。CDN不要・オフライン動作)
- データ保存: ブラウザのlocalStorage(端末・ブラウザごとに保存)
- 共通ナビは左サイドバー(家計簿 / 月次キャッシュフロー / 年次タイムライン / 投資シミュレーション)
- 月次・年次グリッドは表内スクロールで見出し・項目名を固定(Excelのウィンドウ枠固定相当)
- ローカル確認: `python -m http.server 8767`(`.claude/launch.json` に設定済み、`timeline.html` も同じサーバーで開ける)

## 参考資料

- `1a.jpg`〜`6.jpg`: もとになった家計簿スプレッドシート(キャッシュフロー表・家計簿シート)のスクリーンショット
